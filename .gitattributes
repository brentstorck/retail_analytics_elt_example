# Normalize line endings: LF in the repo for everything text, CRLF only for Windows scripts.
* text=auto eol=lf
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# Treat data/binary artifacts as binary if they ever get committed.
*.duckdb binary
*.xlsx binary
